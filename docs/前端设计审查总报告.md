# 🔍 前端设计审查总报告

> 审查日期：2026-07-10  
> 审查范围：`frontend/src/` 下全部 30 个源文件  
> 审查维度：功能 Bug / UI 美观性 / 布局合理性 / 交互体验 / 代码架构  
> 总发现：**144 个问题**（23 🔴 + 73 🟡 + 48 🟢）

---

## 🔴 严重问题（23 个，必须修复）

### 🐛 功能 Bug（8 个）

| # | 文件 | 行号 | 问题 | 修复建议 |
|---|------|------|------|----------|
| B1 | PlaybookPage.cpp | 151 | 死代码：`argsStr.isEmpty()` 分支里再次读取 `s["args_template"]`，结果仍为空 | 改为 `s["args"].toString()` 或删除冗余分支 |
| B2 | SystemPage.cpp | 102 | **"保存修改"按钮接错了槽**：connect 到 `onRefreshConfig`（刷新），而非保存逻辑 | 新增真正的保存槽函数，遍历表格逐项 PUT |
| B3 | ScanPage.cpp | 227-228 | **空指针崩溃**：`item(row,0)->text()` 和 `item(row,3)->text()` 未判空 | 取值前判空 `if (!item) return;` |
| B4 | ExecutionPage.cpp | 177 | **空指针崩溃**：`item(row,0)->text()` 未判空 | 同上 |
| B5 | EvaluatePage.cpp | 200,251 | **空指针崩溃**：`item(row,0)->text()` 未判空 | 同上 |
| B6 | SystemPage.cpp | 188,227 | **空指针崩溃**：`onDeleteUser`/`onDeleteClass` 中 `item(row,0)->text()` 未判空 | 同上 |
| B7 | DeployConfigPage.cpp | 135-143 | **重复 API 调用**：`onRefresh()` 中 `/api/health` 被调 2 次 | 合并为一次调用，回调中同时更新状态和平台信息 |
| B8 | DeployConfigPage.cpp | 135-143 | **重复 API 调用**：`/api/tools/containers/status` 被调 2 次 | 合并为一次调用 |

### 🎨 UI 美观性（4 个）

| # | 文件 | 行号 | 问题 | 修复建议 |
|---|------|------|------|----------|
| U1 | TopologyPage.cpp | 582-598 | **样式冲突**：页面级 setStyleSheet 覆盖全局 QSS，按钮外观与其他页面不一致 | 只覆盖特定子控件，通过对象名精确控制 |
| U2 | 多处 | 多处 | **成功/错误/警告颜色 3 套体系混用**：`green`/`#166534`、`red`/`#991b1b`/`#d63031`、`orange` | 在 GLOBAL_STYLE 中统一定义，各页面统一引用 |
| U3 | 全部 | 全部 | **完全无 QIcon**：所有按钮均为纯文字，导航栏无图标 | 至少为导航栏 7 个模块和主要操作按钮添加图标 |
| U4 | 全部 | 全部 | **无深色模式支持**：国产 OS 可能默认深色主题，硬编码浅色会失去对比度 | 提供 `GLOBAL_STYLE_LIGHT` / `GLOBAL_STYLE_DARK` 两套 QSS |

### 📐 布局合理性（4 个）

| # | 文件 | 行号 | 问题 | 修复建议 |
|---|------|------|------|----------|
| L1 | DeployConfigPage.cpp | 14-131 | **缺 QScrollArea**：6 个区域内容超长，窗口缩小后底部被裁切不可见 | 外层套 QScrollArea |
| L2 | ScanPage.cpp | 14-136 | **缺 QScrollArea**：内容总高远超 700px，底部被裁切 | 外层套 QScrollArea |
| L3 | PlaybookPage.cpp | 20-103 | **缺 QSplitter**：左右分栏固定 500px，用户无法拖动调整比例 | 改为 QSplitter + setStretchFactor |
| L4 | TopologyPage.cpp | 1-1991 | **1990 行巨型文件**：混合 7 个关注点 | 拆分为 4-5 个文件 |

### ♿ 交互体验（4 个）

| # | 文件 | 行号 | 问题 | 修复建议 |
|---|------|------|------|----------|
| I1 | LoginDialog.cpp | 42-53 | **用户名框按 Enter 无反应**：不会跳到密码框 | `connect(m_usernameEdit, &QLineEdit::returnPressed, m_passwordEdit, qOverload<>(&QWidget::setFocus));` |
| I2 | ApiClient.cpp | 22-43 | **无全局错误信号/重试机制**：所有失败被静默忽略 | 添加全局错误信号，MainWindow 状态栏显示 |
| I3 | main.cpp | 169-174 | **后端宕机无离线模式**：应用完全不可用 | 添加重试按钮或离线只读模式 |
| I4 | main.cpp | 157-160 | **假进度条**：硬编码定时器驱动，与真实加载无关 | 连接真实加载事件（字体加载→20%，后端健康→60%，就绪→100%） |

### 🏗️ 代码架构（3 个）

| # | 文件 | 行号 | 问题 | 修复建议 |
|---|------|------|------|----------|
| A1 | 全部 7 页面 | 全部 | **缺 BasePage 基类**：7 个页面重复 48+ 处 API 调用+表格初始化样板代码 | 抽取 BasePage，提供 `setupSectionHeader()`、`createTable()`、`callApi()` 模板方法 |
| A2 | TopologyPage.cpp | 1-1991 | **巨型文件**（同 L4） | 拆分为 TopologyCanvas/TopologyEditor/TopologySceneManager/TopologyDocumentIO |
| A3 | 全项目 | 全项目 | **58 处硬编码颜色值**：修改主题需逐文件搜索替换 | 提取为全局常量或 .qss 文件 |

---

## 🟡 中等问题（73 个，建议修复）

### 🐛 功能 Bug（10 个）

| # | 文件 | 问题摘要 |
|---|------|----------|
| B9 | PlaybookPage.cpp:169 | 删除 Playbook 无确认弹窗 |
| B10 | SystemPage.cpp:185 | 删除用户无确认弹窗 |
| B11 | SystemPage.cpp:224 | 删除班级无确认弹窗 |
| B12 | EvaluatePage.cpp:222 | 删除报告无确认弹窗 |
| B13 | DeployConfigPage.cpp:240 | **异步竞态**：先异步保存 LLM 配置再立即 GET，可能读到旧数据 |
| B14 | PlaybookPage.cpp:219 | 新建 Playbook 对话框验证不足（对话框关闭后才检查空值） |
| B15 | TopologyPage.cpp:986 | 字段名 `teacher_sub` 可能与后端不匹配 |
| B16 | SplashDialog.cpp:15 | Splash 尺寸为屏幕 1/2，偏大 |
| B17 | LoginDialog.cpp:17 | 登录框尺寸为屏幕 1/2，偏大 |
| B18 | TopologyTypes.cpp:102 | 使用已弃用 API（`QRegExp`、`QString::SkipEmptyParts`） |

### 🎨 UI 美观性（9 个）

| # | 文件 | 问题摘要 |
|---|------|----------|
| U5 | 6 个页面文件 | sectionStyle 字符串重复定义 6 次 |
| U6 | 全项目 | 主色 `#2a7dd6` 硬编码 14+ 处 |
| U7 | TopologyPage.cpp:593 | 属性选择器 vs 对象名选择器，两套命名约定不统一 |
| U8 | LoginDialog.cpp | 8 处内联 setStyleSheet |
| U9 | MainWindow.cpp:35 | 导航栏缺少图标 |
| U10 | MainWindow.cpp:34 | 导航栏 220px 固定宽度未适配高 DPI |
| U11 | 全部页面 | 各 section 间距无统一规范 |
| U12 | main.cpp:147 | 字体单位混用（全局 pointSize vs QSS 中 px） |
| U13 | main.cpp:121 | 中文字体回退方案不完善 |

### 📐 布局合理性（21 个）

| # | 文件 | 问题摘要 |
|---|------|----------|
| L5 | ExecutionPage.cpp | 缺 QScrollArea |
| L6 | SystemPage.cpp | QTabWidget 内部缺滚动 |
| L7 | EvaluatePage.cpp | Tab 页内部缺滚动 |
| L8 | LoginDialog.cpp:18 | setFixedSize 不可调整大小 |
| L9 | SplashDialog.cpp:16 | 注释写 1/4 实际是 1/2 |
| L10 | PlaybookPage.cpp:66 | 左侧 setMaximumWidth(500) 固定 |
| L11 | DeployConfigPage.cpp:105 | m_imageTable setMaximumHeight(150) |
| L12 | DeployConfigPage.cpp:115 | m_toolTable setMaximumHeight(200) |
| L13 | ScanPage.cpp:83 | m_taskTable setMaximumHeight(200) |
| L14 | ScanPage.cpp:106 | m_resultTable setMaximumHeight(150) |
| L15 | ScanPage.cpp:117 | m_recTable setMaximumHeight(150) |
| L16 | ExecutionPage.cpp:63 | m_runTable setMaximumHeight(200) |
| L17 | ExecutionPage.cpp:77 | m_stepTable setMaximumHeight(180) |
| L18 | ExecutionPage.cpp:89 | m_evidenceTable setMaximumHeight(150) |
| L19 | ExecutionPage.cpp:93 | m_evidenceDetail setMaximumHeight(120) |
| L20 | EvaluatePage.cpp:62 | m_reportTable setMaximumHeight(200) |
| L21 | EvaluatePage.cpp:101 | m_evidenceTable setMaximumHeight(250) |
| L22 | EvaluatePage.cpp:108 | m_evidenceDetail setMaximumHeight(200) |
| L23 | LoginDialog.cpp:16-18 | 4K 屏幕上对话框 1920x1080 异常巨大 |
| L24 | SplashDialog.cpp:13-16 | 4K 屏幕上闪屏异常巨大 |
| L25 | EvaluatePage.cpp | Tab 页内部表格+QTextEdit 无滚动保护 |
| L26 | SystemPage.cpp | 任务管理 Tab 两个表格堆叠，小窗口裁切 |

### ♿ 交互体验（27 个）

| # | 文件 | 问题摘要 |
|---|------|----------|
| I5 | MainWindow.cpp:67 | 切换页面不触发数据刷新 |
| I6 | DeployConfigPage.cpp:135 | 6 个并发请求无加载遮罩 |
| I7 | ScanPage.cpp:133 | 轮询在页面隐藏时仍运行 |
| I8 | DeployConfigPage.cpp:101-125 | 表格无空状态提示 |
| I9 | PlaybookPage.cpp:44-49 | 表格无空状态提示 |
| I10 | ScanPage.cpp:78,102,112 | 表格无空状态提示 |
| I11 | ExecutionPage.cpp:58,72,84 | 表格无空状态提示 |
| I12 | EvaluatePage.cpp:40,57,97 | 表格无空状态提示 |
| I13 | SystemPage.cpp:30-32 等 | 表格无空状态提示 |
| I14 | SystemPage.cpp:178 | 添加用户无成功反馈 |
| I15 | SystemPage.cpp:185 | 删除用户无确认（同 B10） |
| I16 | SystemPage.cpp:211,224 | 添加/删除班级无反馈/确认 |
| I17 | PlaybookPage.cpp:169 | 删除无确认（同 B9） |
| I18 | EvaluatePage.cpp:222 | 删除无确认（同 B12） |
| I19 | ExecutionPage.cpp:137 | 执行成功无 Toast 提示 |
| I20 | 全部页面 | QLineEdit 未绑定 Enter 键到操作按钮 |
| I21 | LoginDialog.cpp:70 | 登录按钮 setDefault(false)，Enter 不触发登录 |
| I22 | MainWindow.cpp:67 | 页面切换不自动刷新 |
| I23 | ScanPage.cpp:326 | 轮询在页面隐藏时浪费网络 |
| I24 | 各页面多处 | `if (status != "ok") return;` 静默忽略错误 |
| I25 | LoginDialog.cpp:48 | 无"显示/隐藏密码"切换 |
| I26 | LoginDialog.cpp:42 | 无"记住密码"选项 |
| I27 | MainWindow.cpp:77 | 无"退出登录"按钮 |
| I28 | 所有表格 | 未启用排序 |
| I29 | 所有表格 | 未开启多选模式 |
| I30 | 所有表格 | 无右键上下文菜单 |
| I31 | ScanPage.cpp:21 | 目标输入无历史记录 |
| I32 | PlaybookPage.cpp:87 | 目标输入无历史记录 |
| I33 | ExecutionPage.cpp:47 | 目标输入无历史记录 |
| I34 | ScanPage.cpp:22 | 目标输入无 IP 格式校验 |
| I35 | main.cpp:163 | 闪屏固定等 3.1 秒，后端早到也等 |

### 🏗️ 代码架构（6 个）

| # | 文件 | 问题摘要 |
|---|------|----------|
| A4 | CMakeLists.txt | Qt5::Sql 已链接但未使用 |
| A5 | 多个 .h 文件 | 约 15 处可用前向声明替代 #include |
| A6 | TopologyPage.h | `buildUi()` vs 其余页面 `setupUI()` 命名不一致 |
| A7 | TopologyPage.h:37-40 | 槽函数未使用 `on` 前缀约定 |
| A8 | ApiClient.cpp:29 | 成功路径未规范化 `status:"ok"`，依赖服务端返回 |
| A9 | 全部页面 | 错误处理策略不统一（有的静默，有的显示） |

---

## 🟢 轻微问题（48 个，可选优化）

### 🐛 功能 Bug（6 个）

| # | 问题 |
|---|------|
| B19 | ApiClient lambda 捕获 this，当前安全但缺 QPointer 保护 |
| B20 | 各 Page lambda 捕获 this，动态页面场景下有风险 |
| B21 | ScanPage 轮询定时器页面切换时不停止（浪费资源） |
| B22 | DeployConfigPage onRefresh 发起 7+ 个并行请求 |
| B23 | LoginDialog 栈对象异步回调安全性依赖 exec() 阻塞 |
| B24 | SystemPage 构造时发起 6 个并行请求 |

### 🎨 UI 美观性（7 个）

| # | 问题 |
|---|------|
| U14 | SplashDialog 4 处内联样式可接受，进度条样式应统一到全局 |
| U15 | PlaybookPage "新建"按钮内联样式应改为对象名 |
| U16 | 导航栏选中项缺左侧竖线指示器 |
| U17 | 表头深色与表格体对比过强 |
| U18 | TopologyPage 画布深色背景与浅色面板过渡突兀 |
| U19 | 内联 emoji ✅❌ 在不同 OS 渲染不一致 |
| U20 | ScanPage "执行扫描"按钮内联样式重复全局定义 |

### 📐 布局合理性（11 个）

| # | 问题 |
|---|------|
| L27 | SplashDialog 注释写 1/4 实际 1/2 |
| L28 | DeployConfigPage 模型输入框 setMaximumWidth(200) 可能截断 |
| L29 | MainWindow 未设 setMinimumSize，窗口可缩到很小 |
| L30 | LoginDialog 输入框 FixedHeight 42px 与全局 min-height 28px 不一致 |
| L31 | PlaybookPage 新建对话框描述框 maxHeight 60px 偏小 |
| L32 | 全局 QPushButton/QLineEdit min-height 已合理 |
| L33-L37 | EvaluatePage/SystemPage Tab 页内部滚动问题 |

### ♿ 交互体验（12 个）

| # | 问题 |
|---|------|
| I36 | LoginDialog "退出"按钮无确认 |
| I37 | MainWindow 后端检测失败无自动重试 |
| I38 | PlaybookPage 快捷目标输入 Enter 不触发执行 |
| I39 | 所有表格列宽不可交互调整 |
| I40-I45 | 各页面表格无空状态提示（6 处） |

### 🏗️ 代码架构（12 个）

| # | 问题 |
|---|------|
| A10 | GLOBAL_STYLE 与主程序逻辑在同一文件，建议分离为 .qss |
| A11 | 全项目 new 控件/QTableWidgetItem 内存管理正确，无泄漏 |
| A12 | QNetworkReply 统一 deleteLater，正确 |
| A13 | Lambda 捕获 this 当前安全（页面不销毁） |
| A14 | PlaceholderPage 完全未使用，应删除 |
| A15 | resources.qrc 无废弃资源 |
| A16 | 信号连接已统一使用新式函数指针语法 ✅ |
| A17-A22 | 约 6 个 .h 文件可用前向声明优化 |

---

## 📊 统计总览

| 维度 | 🔴 | 🟡 | 🟢 | 小计 |
|------|----|----|----|----|
| 🐛 功能 Bug | 8 | 10 | 6 | 24 |
| 🎨 UI 美观性 | 4 | 9 | 7 | 20 |
| 📐 布局合理性 | 4 | 21 | 11 | 36 |
| ♿ 交互体验 | 4 | 27 | 12 | 43 |
| 🏗️ 代码架构 | 3 | 6 | 12 | 21 |
| **合计** | **23** | **73** | **48** | **144** |

---

## 🎯 修复优先级建议

### P0 — 立即修复（影响稳定性/可用性）

1. **B3-B6** — 空指针崩溃（5 处 `QTableWidget::item()` 未判空）→ 直接导致 crash
2. **B2** — "保存修改"按钮接错槽 → 功能完全失效
3. **B7-B8** — 重复 API 调用 → 浪费网络，可能数据竞争
4. **I1** — 登录框 Enter 键无反应 → 阻碍基本操作流程

### P1 — 本迭代修复（影响用户体验）

5. **L1-L2** — DeployConfigPage/ScanPage 缺 QScrollArea → 内容不可见
6. **L11-L22** — 全部 12 处表格 setMaximumHeight → 信息密度极低
7. **B9-B12** — 4 处删除无确认弹窗 → 误操作不可撤销
8. **U2** — 成功/错误/警告颜色 3 套体系混用
9. **I4** — 假进度条损害用户信任
10. **B13** — LLM 测试连接异步竞态

### P2 — 下迭代修复（架构改进）

11. **A1** — 抽取 BasePage 基类（消除 48+ 处重复代码）
12. **A2/L4** — TopologyPage.cpp 拆分（1990→4-5 文件）
13. **A3** — 58 处硬编码颜色提取为常量
14. **U4** — 深色模式支持（国产 OS 必需）
15. **U3** — 添加图标（导航栏 + 操作按钮）

### P3 — 持续优化

16. **I5/I22** — 页面切换自动刷新
17. **I28-I30** — 表格排序/多选/右键菜单
18. **I31-I34** — 输入历史记录 + 格式校验
19. **U5** — sectionStyle 统一提取
20. **A4** — CMake 清理（移除 Qt5::Sql + PlaceholderPage）
