# ── Build artifacts ──────────────────────────
build/
cmake-build-*/
dist/
*.autosave
*.user
*.o
*.so
*.a
moc_*
ui_*
qrc_*

# ── Node.js ─────────────────────────────────
node_modules/

# ── SQLite runtime files ────────────────────
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db:ro

# ── Data: large/downloaded content ──────────
data/nuclei-templates/
data/output/

# ── Archives ────────────────────────────────
*.tar
*.tar.gz
*.tar.xz

# ── Secrets ─────────────────────────────────
pentagi_key
*.pem
*.key

# ── Claude Code local config ────────────────
.claude/

# ── Container binaries (too large for git) ──
containers/recon/binaries/

# ── One-off scripts (not part of project) ───
check_*.mjs
fix_data.mjs
pentagi-v3/
